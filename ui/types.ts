export type User = {
    id: string,
    username: string,
    first_name: string,
    last_name: string,
    email:string, 
    is_superuser: boolean,
    is_staff: boolean,
    is_active: boolean
  }